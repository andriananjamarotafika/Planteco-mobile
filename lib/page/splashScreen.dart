import 'package:flutter/material.dart';

class SpalshScreenPage extends StatelessWidget {
  const SpalshScreenPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}